# Server Configuration
PORT=5000
NODE_ENV=development

# Database
MONGODB_URI=

# JWT
JWT_SECRET=dev_secret_key_change_in_production
JWT_EXPIRY=7d

# CORS
FRONTEND_URL=http://localhost:5173


HF_TOKEN = go to huggingface.co and create a token, then paste it here. It is needed for the AI categorization feature.